# #/components/schemas/create_user_response
description: "schema for create new user response, containing newly created user"
type: "object"
required:
  - "data"
properties:
  data:
    $ref: "#/components/schemas/user_with_personal_info"