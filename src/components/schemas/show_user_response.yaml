# #/components/schemas/show_user_response
description: "schema for show user response corresponding to non-self user"
type: "object"
required:
  - "data"
properties:
  data:
    $ref: "#/components/schemas/user"