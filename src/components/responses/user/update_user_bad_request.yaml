# #/components/responses/update_user_bad_request
description: "bad update user request, `source` will point to attribute generating error"
content:
  application/vnd.json+api:
    schema:
      $ref: "#/components/schemas/error_response"
    examples:
      invalid_attribute_error:
        $ref: "#/components/examples/user_invalid_attribute_error"
      non_unique_attribute_error:
        $ref: "#/components/examples/user_non_unique_attribute_error"
      implausible_birthday_error:
        $ref: "#/components/examples/user_implausible_birthday_error"