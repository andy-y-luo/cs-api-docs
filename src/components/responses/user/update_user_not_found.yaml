# #/components/responses/update_user_not_found
description: the user to update does not exist
content:
  application/vnd.json+api:
    schema:
      $ref: "#/components/schemas/error_response"
    examples:
      user_does_not_exist_error:
        $ref: "#/components/examples/user_user_does_not_exist_error"